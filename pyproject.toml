[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "phanotate-rs"
version = "0.1.3"
description = "Python bindings for PHANOTATE gene finder"
requires-python = ">=3.8"

[tool.maturin]
manifest-path = "Cargo.toml"
features = ["python"]
